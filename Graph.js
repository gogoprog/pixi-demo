import createGraph from "ngraph.graph";

 export default function Graph() {
    // this.ngraph = createGraph();
    // createGraph.apply(this, arguments);
    var ngraph = createGraph();
    return ngraph;
}
// Graph.prototype.ngraph = createGraph();

